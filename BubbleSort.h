#pragma once
#include <stdio.h>
#include <time.h>
#include <math.h>
#include <iostream>
#include <algorithm>
#include <vector>

using namespace std;


class BubbleSort
{
public:
	void bubbleSort(int arr[], int n);
	void printArray(int arr[], int size);
};